# Close Excel Session

Closes the current Excel session.

## Usage

``` r
closeXl(save = FALSE)
```

## Arguments

- save:

  logical should the save dialog be displayed?

## Value

Invisibly returns `NULL`.
