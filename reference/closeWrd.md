# Close Word Session

Closes the current Word session and optionally saves open documents.

## Usage

``` r
closeWrd(save = FALSE)
```

## Arguments

- save:

  Logical; whether to save documents before closing.

## Value

Invisibly returns `NULL`.
