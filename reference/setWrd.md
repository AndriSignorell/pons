# Set Current Word Session

Registers a Word COM object as the current default session.

## Usage

``` r
setWrd(wrd)
```

## Arguments

- wrd:

  A Word COM object (class `"COMIDispatch"`).

## Value

Invisibly returns `wrd`.
