% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlView-glm.R, R/xlView-lm.R, R/xlView.R
\name{xlView.glm}
\alias{xlView.glm}
\alias{xlView.lm}
\alias{xlView}
\alias{xlView.data.frame}
\alias{xlView.matrix}
\alias{xlView.table}
\alias{xlView.array}
\alias{xlView.default}
\alias{xlView.list}
\title{View Objects in Excel}
\usage{
\method{xlView}{glm}(
  x,
  sheet = NULL,
  conf.level = NA,
  method = c("profile", "wald"),
  exponentiate = FALSE,
  anova = FALSE,
  fitted = FALSE,
  autofit = TRUE,
  gap = 2L,
  xl = NULL,
  ...
)

\method{xlView}{lm}(
  x,
  sheet = NULL,
  conf.level = NA,
  anova = FALSE,
  fitted = FALSE,
  autofit = TRUE,
  gap = 2L,
  xl = NULL,
  ...
)

xlView(x, sheet = NULL, ...)

\method{xlView}{data.frame}(
  x,
  sheet = NULL,
  rowNames = FALSE,
  table = TRUE,
  autofit = TRUE,
  freeze = TRUE,
  xl = NULL,
  ...
)

\method{xlView}{matrix}(
  x,
  sheet = NULL,
  rowNames = NA,
  table = TRUE,
  autofit = TRUE,
  freeze = TRUE,
  xl = NULL,
  ...
)

\method{xlView}{table}(
  x,
  sheet = NULL,
  rowNames = NA,
  table = TRUE,
  autofit = TRUE,
  freeze = TRUE,
  xl = NULL,
  ...
)

\method{xlView}{array}(
  x,
  sheet = NULL,
  rowNames = NA,
  table = TRUE,
  autofit = TRUE,
  freeze = TRUE,
  xl = NULL,
  ...
)

\method{xlView}{default}(
  x,
  sheet = NULL,
  rowNames = NA,
  table = TRUE,
  autofit = TRUE,
  freeze = TRUE,
  xl = NULL,
  ...
)

\method{xlView}{list}(
  x,
  sheet = NULL,
  rowNames = NA,
  autofit = TRUE,
  gap = 2L,
  titles = TRUE,
  xl = NULL,
  ...
)
}
\arguments{
\item{x}{an object to be displayed: a data frame, a matrix or two-dimensional
table, an atomic vector (with or without names) or a possibly nested list
of these}

\item{sheet}{optional worksheet name; defaults to the deparsed name of
\code{x}}

\item{conf.level}{confidence level for the coefficient intervals, or
\code{NA} (default) to omit them}

\item{method}{the procedure for the coefficient intervals, either
\code{"profile"} (profile likelihood, the default of \code{confint.glm})
or \code{"wald"}}

\item{exponentiate}{logical; if \code{TRUE}, the estimates and their
interval bounds are additionally reported on the exponentiated scale
(odds ratios for a logit link, rate ratios for a log link)}

\item{anova}{logical; if \code{TRUE}, the sequential analysis of variance
table is appended}

\item{fitted}{logical; if \code{TRUE}, the model frame together with fitted
values and residuals is appended}

\item{autofit}{logical; if \code{TRUE}, column widths are adjusted
automatically}

\item{gap}{integer, list method only; number of empty rows between two
consecutive blocks}

\item{xl}{optional Excel COM object}

\item{\dots}{further arguments, currently ignored}

\item{rowNames}{logical or \code{NA}; if \code{TRUE}, row names (resp. the
names of a vector) are written as a leading column. \code{NA} means
automatic: names are written whenever the object carries them.}

\item{table}{logical; if \code{TRUE}, Excel filters are enabled. Ignored when
the object has no header row and for lists.}

\item{freeze}{logical; if \code{TRUE}, the first row is frozen. Ignored when
there is no header row and for lists.}

\item{titles}{logical, list method only; if \code{TRUE}, the element names
are written as a bold label above each block}
}
\value{
Invisibly returns the worksheet COM object.
}
\description{
Opens a data frame, a matrix, an atomic vector or a list of such objects in
Microsoft Excel for interactive inspection.
}
\details{
A new workbook and worksheet are created automatically.

Data are transferred column-wise to preserve numeric types. Factors and
date-time objects are converted to character vectors.

Matrices are written with those dimnames they actually have: a matrix without
column names gets no header row, a matrix without row names gets no leading
name column. Atomic vectors are written as a single column headed by the
deparsed expression.

Lists are stacked vertically on one sheet, each element separated by
\code{gap} empty rows. Nested lists are indented by one column. Since Excel
allows only one autofilter per sheet, \code{table} and \code{freeze} are not
available for lists.

The workbook is not saved automatically.
}
\examples{
\dontrun{
xlView(iris)
xlView(as.matrix(mtcars))
xlView(table(d.pizza$driver, d.pizza$area))
xlView(rnorm(10))
xlView(list(summary = summary(iris$Sepal.Length),
            counts  = table(iris$Species),
            data    = head(iris)))
}

}
\concept{data-inspection}
\concept{excel}
\concept{spreadsheet}
\concept{spreadsheet.utils}
